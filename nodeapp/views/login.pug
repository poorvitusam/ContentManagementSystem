extends layout

block content
	h1 Welcome to #{title}
	h2 Enter name and password to login!
	form(method = 'post' action='/login')
		div 
			input(type='text' name='username' placeholder='Username')
		div
			input(type='password' name='password' placeholder='Password')
		input(type='submit')

	div(ng-view)